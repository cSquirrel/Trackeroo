{
  "name": "trackeroo-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Playwright end-to-end tests for Trackeroo, run against the full Docker stack.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:smoke": "playwright test --grep @smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^24.13.2"
  }
}
