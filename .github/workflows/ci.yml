name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  backend-tests:
    name: Backend (pytest + coverage)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: v1/backend/requirements.txt
      - name: Install dependencies
        run: pip install -r v1/backend/requirements.txt
      - name: Run tests with coverage
        working-directory: v1/backend
        run: pytest --cov=app --cov-report=term-missing --cov-report=xml
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: v1/backend/coverage.xml
          if-no-files-found: warn

  frontend-tests:
    name: Frontend (check + test + build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: v1/frontend/package-lock.json
      - name: Install dependencies
        working-directory: v1/frontend
        run: npm ci
      - name: Type-check
        working-directory: v1/frontend
        run: npm run check
      - name: Unit / component tests
        working-directory: v1/frontend
        run: npm run test
      - name: Build
        working-directory: v1/frontend
        run: npm run build

  mcp-tests:
    name: MCP integration (Docker)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            v1/mcp/requirements.txt
            v1/mcp/tests/requirements.txt
      - name: Verify Docker is available
        run: docker compose version
      - name: Install dependencies
        run: pip install -r v1/mcp/requirements.txt -r v1/mcp/tests/requirements.txt
      - name: Run MCP integration tests
        working-directory: v1/mcp
        run: pytest -q

  e2e-smoke:
    name: E2E smoke (Playwright)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: v1/e2e/package-lock.json
      - name: Verify Docker is available
        run: docker compose version
      - name: Install dependencies
        working-directory: v1/e2e
        run: npm install
      - name: Install Playwright browser
        working-directory: v1/e2e
        run: npx playwright install chromium --with-deps
      - name: Run smoke suite
        working-directory: v1/e2e
        run: npm run test:e2e:smoke

  e2e-full:
    name: E2E full regression (Playwright)
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: v1/e2e/package-lock.json
      - name: Verify Docker is available
        run: docker compose version
      - name: Install dependencies
        working-directory: v1/e2e
        run: npm install
      - name: Install Playwright browser
        working-directory: v1/e2e
        run: npx playwright install chromium --with-deps
      - name: Run full suite
        working-directory: v1/e2e
        run: npm run test:e2e
