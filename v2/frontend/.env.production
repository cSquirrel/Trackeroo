# Loaded by `vite build` (used by `tauri build`). The bundled webview loads via
# Tauri's asset scheme, so the API base must be the sidecar's absolute URL.
VITE_API_BASE_URL=http://localhost:8787/api
